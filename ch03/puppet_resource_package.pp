package { 'CFPropertyList':
  ensure   => ['2.3.6'],
  provider => 'gem',
}

package { 'addressable':
  ensure   => ['2.8.0'],
  provider => 'gem',
}

package { 'ansi':
  ensure   => ['1.5.0'],
  provider => 'gem',
}

package { 'ast':
  ensure   => ['2.4.2'],
  provider => 'gem',
}

package { 'astroid':
  ensure   => '2.9.3',
  provider => 'pip',
}

package { 'awesome_print':
  ensure   => ['1.9.2'],
  provider => 'gem',
}

package { 'aws-eventstream':
  ensure   => ['1.2.0'],
  provider => 'gem',
}

package { 'aws-partitions':
  ensure   => ['1.525.0'],
  provider => 'gem',
}

package { 'aws-sdk-core':
  ensure   => ['3.122.0'],
  provider => 'gem',
}

package { 'aws-sdk-ec2':
  ensure   => ['1.277.0'],
  provider => 'gem',
}

package { 'aws-sigv4':
  ensure   => ['1.4.0'],
  provider => 'gem',
}

package { 'bcrypt_pbkdf':
  ensure   => ['1.1.0'],
  provider => 'gem',
}

package { 'benchmark':
  ensure   => ['0.1.0'],
  provider => 'gem',
}

package { 'bigdecimal':
  ensure   => ['2.0.0'],
  provider => 'gem',
}

package { 'bindata':
  ensure   => ['2.4.10'],
  provider => 'gem',
}

package { 'bolt':
  ensure   => ['3.20.0'],
  provider => 'gem',
}

package { 'builder':
  ensure   => ['3.2.4'],
  provider => 'gem',
}

package { 'bundler':
  ensure   => ['2.1.4'],
  provider => 'gem',
}

package { 'byebug':
  ensure   => ['11.1.3'],
  provider => 'gem',
}

package { 'cgi':
  ensure   => ['0.1.0'],
  provider => 'gem',
}

package { 'cmath':
  ensure   => ['1.0.0'],
  provider => 'puppet_gem',
}

package { 'codecov':
  ensure   => ['0.6.0'],
  provider => 'gem',
}

package { 'coderay':
  ensure   => ['1.1.3'],
  provider => 'gem',
}

package { 'colored2':
  ensure   => ['3.1.2'],
  provider => 'gem',
}

package { 'concurrent-ruby':
  ensure   => ['1.1.9', '1.1.5'],
  provider => 'gem',
}

package { 'connection_pool':
  ensure   => ['2.2.5'],
  provider => 'gem',
}

package { 'cri':
  ensure   => ['2.15.10'],
  provider => 'gem',
}

package { 'csv':
  ensure   => ['3.1.2'],
  provider => 'gem',
}

package { 'date':
  ensure   => ['3.0.0'],
  provider => 'gem',
}

package { 'dbm':
  ensure   => ['1.1.0'],
  provider => 'gem',
}

package { 'deep_merge':
  ensure   => ['1.2.1'],
  provider => 'gem',
}

package { 'delegate':
  ensure   => ['0.1.0'],
  provider => 'gem',
}

package { 'dependency_checker':
  ensure   => ['0.2.0'],
  provider => 'gem',
}

package { 'did_you_mean':
  ensure   => ['1.4.0'],
  provider => 'gem',
}

package { 'diff-lcs':
  ensure   => ['1.4.4'],
  provider => 'gem',
}

package { 'docile':
  ensure   => ['1.4.0'],
  provider => 'gem',
}

package { 'docker-api':
  ensure   => ['2.2.0'],
  provider => 'gem',
}

package { 'domain_name':
  ensure   => ['0.5.20190701'],
  provider => 'gem',
}

package { 'ed25519':
  ensure   => ['1.2.4'],
  provider => 'gem',
}

package { 'erubi':
  ensure   => ['1.10.0'],
  provider => 'gem',
}

package { 'etc':
  ensure   => ['1.1.0'],
  provider => 'gem',
}

package { 'excon':
  ensure   => ['0.88.0'],
  provider => 'gem',
}

package { 'facter':
  ensure   => ['4.2.5', '2.5.1'],
  provider => 'gem',
}

package { 'facterdb':
  ensure   => ['1.11.0'],
  provider => 'gem',
}

package { 'faraday':
  ensure   => ['0.17.4'],
  provider => 'gem',
}

package { 'faraday_middleware':
  ensure   => ['0.14.0'],
  provider => 'gem',
}

package { 'fast_gettext':
  ensure   => ['1.8.0', '1.1.2'],
  provider => 'gem',
}

package { 'fcntl':
  ensure   => ['1.0.0'],
  provider => 'gem',
}

package { 'ffi':
  ensure   => ['1.15.4'],
  provider => 'gem',
}

package { 'ffi-compiler':
  ensure   => ['1.0.1'],
  provider => 'gem',
}

package { 'fiddle':
  ensure   => ['1.0.0'],
  provider => 'gem',
}

package { 'fileutils':
  ensure   => ['1.4.1'],
  provider => 'gem',
}

package { 'formatador':
  ensure   => ['0.3.0'],
  provider => 'gem',
}

package { 'forwardable':
  ensure   => ['1.3.1'],
  provider => 'gem',
}

package { 'getoptlong':
  ensure   => ['0.1.0'],
  provider => 'gem',
}

package { 'gettext':
  ensure   => ['3.2.9'],
  provider => 'gem',
}

package { 'gettext-setup':
  ensure   => ['0.34'],
  provider => 'gem',
}

package { 'gssapi':
  ensure   => ['1.3.1'],
  provider => 'gem',
}

package { 'guard':
  ensure   => ['2.18.0'],
  provider => 'gem',
}

package { 'gyoku':
  ensure   => ['1.3.1'],
  provider => 'gem',
}

package { 'hiera':
  ensure   => ['3.7.0'],
  provider => 'gem',
}

package { 'hiera-eyaml':
  ensure   => ['3.2.2'],
  provider => 'gem',
}

package { 'highline':
  ensure   => ['2.0.3'],
  provider => 'gem',
}

package { 'hirb':
  ensure   => ['0.7.3'],
  provider => 'gem',
}

package { 'hocon':
  ensure   => ['1.3.1'],
  provider => 'gem',
}

package { 'honeycomb-beeline':
  ensure   => ['2.7.1'],
  provider => 'gem',
}

package { 'http':
  ensure   => ['4.4.1'],
  provider => 'gem',
}

package { 'http-accept':
  ensure   => ['1.7.0'],
  provider => 'gem',
}

package { 'http-cookie':
  ensure   => ['1.0.4'],
  provider => 'gem',
}

package { 'http-form_data':
  ensure   => ['2.3.0'],
  provider => 'gem',
}

package { 'http-parser':
  ensure   => ['1.2.3'],
  provider => 'gem',
}

package { 'httpclient':
  ensure   => ['2.8.3'],
  provider => 'gem',
}

package { 'io-console':
  ensure   => ['0.5.6'],
  provider => 'gem',
}

package { 'ipaddr':
  ensure   => ['1.2.2'],
  provider => 'gem',
}

package { 'irb':
  ensure   => ['1.2.6'],
  provider => 'gem',
}

package { 'isort':
  ensure   => '5.10.1',
  provider => 'pip',
}

package { 'jgrep':
  ensure   => ['1.5.4'],
  provider => 'gem',
}

package { 'jmespath':
  ensure   => ['1.4.0'],
  provider => 'gem',
}

package { 'json':
  ensure   => ['2.3.0'],
  provider => 'gem',
}

package { 'json-schema':
  ensure   => ['2.8.1'],
  provider => 'gem',
}

package { 'json_pure':
  ensure   => ['2.5.1'],
  provider => 'gem',
}

package { 'jwt':
  ensure   => ['2.2.3'],
  provider => 'gem',
}

package { 'lazy-object-proxy':
  ensure   => '1.7.1',
  provider => 'pip',
}

package { 'libhoney':
  ensure   => ['1.21.0'],
  provider => 'gem',
}

package { 'license_finder':
  ensure   => ['6.14.2'],
  provider => 'gem',
}

package { 'listen':
  ensure   => ['3.7.0'],
  provider => 'gem',
}

package { 'little-plugger':
  ensure   => ['1.1.4'],
  provider => 'gem',
}

package { 'locale':
  ensure   => ['2.1.3'],
  provider => 'gem',
}

package { 'log4r':
  ensure   => ['1.1.10'],
  provider => 'gem',
}

package { 'logger':
  ensure   => ['1.4.2'],
  provider => 'gem',
}

package { 'logging':
  ensure   => ['2.3.0'],
  provider => 'gem',
}

package { 'lumberjack':
  ensure   => ['1.2.8'],
  provider => 'gem',
}

package { 'matrix':
  ensure   => ['0.2.0'],
  provider => 'gem',
}

package { 'mccabe':
  ensure   => '0.6.1',
  provider => 'pip',
}

package { 'metaclass':
  ensure   => ['0.0.4'],
  provider => 'gem',
}

package { 'metadata-json-lint':
  ensure   => ['3.0.1'],
  provider => 'gem',
}

package { 'method_source':
  ensure   => ['1.0.0'],
  provider => 'gem',
}

package { 'mime-types':
  ensure   => ['3.3.1'],
  provider => 'gem',
}

package { 'mime-types-data':
  ensure   => ['3.2021.0901'],
  provider => 'gem',
}

package { 'mini_portile2':
  ensure   => ['2.4.0'],
  provider => 'gem',
}

package { 'minitar':
  ensure   => ['0.9'],
  provider => 'gem',
}

package { 'minitest':
  ensure   => ['5.13.0'],
  provider => 'gem',
}

package { 'mocha':
  ensure   => ['1.1.0'],
  provider => 'gem',
}

package { 'molinillo':
  ensure   => ['0.8.0'],
  provider => 'gem',
}

package { 'multi_json':
  ensure   => ['1.15.0'],
  provider => 'gem',
}

package { 'multipart-post':
  ensure   => ['2.1.1'],
  provider => 'gem',
}

package { 'munkitools-3.1.1.3447':
  ensure   => 'installed',
  provider => 'pkgdmg',
}

package { 'mutex_m':
  ensure   => ['0.1.0'],
  provider => 'gem',
}

package { 'nenv':
  ensure   => ['0.3.0'],
  provider => 'gem',
}

package { 'net-http-persistent':
  ensure   => ['4.0.1'],
  provider => 'gem',
}

package { 'net-pop':
  ensure   => ['0.1.0'],
  provider => 'gem',
}

package { 'net-scp':
  ensure   => ['1.2.1'],
  provider => 'gem',
}

package { 'net-smtp':
  ensure   => ['0.1.0'],
  provider => 'gem',
}

package { 'net-ssh':
  ensure   => ['6.1.0'],
  provider => 'gem',
}

package { 'net-ssh-krb':
  ensure   => ['0.5.1'],
  provider => 'gem',
}

package { 'net-telnet':
  ensure   => ['0.2.0', '0.1.1'],
  provider => 'gem',
}

package { 'netrc':
  ensure   => ['0.11.0'],
  provider => 'gem',
}

package { 'nokogiri':
  ensure   => ['1.10.10'],
  provider => 'gem',
}

package { 'nori':
  ensure   => ['2.6.0'],
  provider => 'gem',
}

package { 'notiffany':
  ensure   => ['0.1.3'],
  provider => 'gem',
}

package { 'observer':
  ensure   => ['0.1.0'],
  provider => 'gem',
}

package { 'oga':
  ensure   => ['2.15'],
  provider => 'gem',
}

package { 'open3':
  ensure   => ['0.1.0'],
  provider => 'gem',
}

package { 'openssl':
  ensure   => ['2.1.2'],
  provider => 'gem',
}

package { 'optimist':
  ensure   => ['3.0.1'],
  provider => 'gem',
}

package { 'orchestrator_client':
  ensure   => ['0.5.2'],
  provider => 'gem',
}

package { 'ostruct':
  ensure   => ['0.2.0'],
  provider => 'gem',
}

package { 'parallel':
  ensure   => ['1.21.0'],
  provider => 'gem',
}

package { 'parallel_tests':
  ensure   => ['3.7.3'],
  provider => 'gem',
}

package { 'parser':
  ensure   => ['3.0.2.0'],
  provider => 'gem',
}

package { 'pathspec':
  ensure   => ['1.0.0'],
  provider => 'gem',
}

package { 'pip':
  ensure   => '21.3.1',
  provider => 'pip',
}

package { 'platformdirs':
  ensure   => '2.4.1',
  provider => 'pip',
}

package { 'pluginator':
  ensure   => ['1.5.0'],
  provider => 'gem',
}

package { 'power_assert':
  ensure   => ['1.1.7'],
  provider => 'gem',
}

package { 'prime':
  ensure   => ['0.1.1'],
  provider => 'gem',
}

package { 'pry':
  ensure   => ['0.14.1'],
  provider => 'gem',
}

package { 'pstore':
  ensure   => ['0.1.0'],
  provider => 'gem',
}

package { 'psych':
  ensure   => ['3.1.0'],
  provider => 'gem',
}

package { 'public_suffix':
  ensure   => ['4.0.6'],
  provider => 'gem',
}

package { 'puppet':
  ensure   => ['7.12.0', '7.11.0', '7.10.0', '7.9.0', '7.8.0', '7.7.0', '7.6.1', '7.5.0', '7.4.1', '7.3.0', '7.1.0', '7.0.0'],
  provider => 'gem',
}

package { 'puppet-blacksmith':
  ensure   => ['6.1.1'],
  provider => 'gem',
}

package { 'puppet-debugger':
  ensure   => ['1.2.0'],
  provider => 'gem',
}

package { 'puppet-lint':
  ensure   => ['2.5.2'],
  provider => 'gem',
}

package { 'puppet-module-posix-default-r2.7':
  ensure   => ['1.1.1'],
  provider => 'gem',
}

package { 'puppet-module-posix-dev-r2.7':
  ensure   => ['1.1.1'],
  provider => 'gem',
}

package { 'puppet-module-posix-system-r2.7':
  ensure   => ['1.1.1'],
  provider => 'gem',
}

package { 'puppet-modulebuilder':
  ensure   => ['0.3.0'],
  provider => 'gem',
}

package { 'puppet-resource_api':
  ensure   => ['1.8.14'],
  provider => 'gem',
}

package { 'puppet-strings':
  ensure   => ['2.8.0'],
  provider => 'gem',
}

package { 'puppet-syntax':
  ensure   => ['3.1.0'],
  provider => 'gem',
}

package { 'puppet_forge':
  ensure   => ['2.3.4'],
  provider => 'gem',
}

package { 'puppet_litmus':
  ensure   => ['0.30.0'],
  provider => 'gem',
}

package { 'puppetfile-resolver':
  ensure   => ['0.5.0'],
  provider => 'gem',
}

package { 'puppetlabs_spec_helper':
  ensure   => ['3.0.0'],
  provider => 'gem',
}

package { 'pylint':
  ensure   => '2.12.2',
  provider => 'pip',
}

package { 'r10k':
  ensure   => ['3.13.0'],
  provider => 'gem',
}

package { 'racc':
  ensure   => ['1.4.16'],
  provider => 'gem',
}

package { 'rainbow':
  ensure   => ['2.2.2'],
  provider => 'gem',
}

package { 'rake':
  ensure   => ['13.0.1', '12.3.3'],
  provider => 'gem',
}

package { 'rb-fsevent':
  ensure   => ['0.11.0'],
  provider => 'gem',
}

package { 'rb-inotify':
  ensure   => ['0.10.1'],
  provider => 'gem',
}

package { 'rb-readline':
  ensure   => ['0.5.5'],
  provider => 'gem',
}

package { 'rdoc':
  ensure   => ['6.2.1'],
  provider => 'gem',
}

package { 'readline':
  ensure   => ['0.0.2'],
  provider => 'gem',
}

package { 'readline-ext':
  ensure   => ['0.1.0'],
  provider => 'gem',
}

package { 'regexp_parser':
  ensure   => ['2.1.1'],
  provider => 'gem',
}

package { 'reline':
  ensure   => ['0.1.5'],
  provider => 'gem',
}

package { 'rest-client':
  ensure   => ['2.1.0'],
  provider => 'gem',
}

package { 'retryable':
  ensure   => ['3.0.5'],
  provider => 'gem',
}

package { 'rexml':
  ensure   => ['3.2.5', '3.2.3.1'],
  provider => 'gem',
}

package { 'rgen':
  ensure   => ['0.9.0'],
  provider => 'gem',
}

package { 'rspec':
  ensure   => ['3.10.0'],
  provider => 'gem',
}

package { 'rspec-core':
  ensure   => ['3.10.1'],
  provider => 'gem',
}

package { 'rspec-expectations':
  ensure   => ['3.10.1'],
  provider => 'gem',
}

package { 'rspec-its':
  ensure   => ['1.3.0'],
  provider => 'gem',
}

package { 'rspec-mocks':
  ensure   => ['3.10.2'],
  provider => 'gem',
}

package { 'rspec-puppet':
  ensure   => ['2.10.0'],
  provider => 'gem',
}

package { 'rspec-puppet-facts':
  ensure   => ['2.0.3'],
  provider => 'gem',
}

package { 'rspec-support':
  ensure   => ['3.10.3'],
  provider => 'gem',
}

package { 'rspec_honeycomb_formatter':
  ensure   => ['0.2.1'],
  provider => 'gem',
}

package { 'rspec_junit_formatter':
  ensure   => ['0.4.1'],
  provider => 'gem',
}

package { 'rss':
  ensure   => ['0.2.8'],
  provider => 'gem',
}

package { 'rubocop':
  ensure   => ['1.6.1'],
  provider => 'gem',
}

package { 'rubocop-ast':
  ensure   => ['1.13.0'],
  provider => 'gem',
}

package { 'rubocop-performance':
  ensure   => ['1.9.1'],
  provider => 'gem',
}

package { 'rubocop-rspec':
  ensure   => ['2.0.1'],
  provider => 'gem',
}

package { 'ruby-ll':
  ensure   => ['2.1.2'],
  provider => 'gem',
}

package { 'ruby-progressbar':
  ensure   => ['1.11.0'],
  provider => 'gem',
}

package { 'ruby_smb':
  ensure   => ['1.1.0'],
  provider => 'gem',
}

package { 'rubygems-update':
  ensure   => ['3.1.4'],
  provider => 'gem',
}

package { 'rubyntlm':
  ensure   => ['0.6.3'],
  provider => 'gem',
}

package { 'rubyzip':
  ensure   => ['2.3.2'],
  provider => 'gem',
}

package { 'sal_scripts-2.0.3.pkg':
  ensure   => 'installed',
  provider => 'pkgdmg',
}

package { 'scanf':
  ensure   => ['1.0.0'],
  provider => 'gem',
}

package { 'sdbm':
  ensure   => ['1.0.0'],
  provider => 'gem',
}

package { 'semantic_puppet':
  ensure   => ['1.0.4'],
  provider => 'gem',
}

package { 'serverspec':
  ensure   => ['2.41.8'],
  provider => 'gem',
}

package { 'setuptools':
  ensure   => '41.2.0',
  provider => 'pip',
}

package { 'sfl':
  ensure   => ['2.3'],
  provider => 'gem',
}

package { 'shellany':
  ensure   => ['0.0.1'],
  provider => 'gem',
}

package { 'simplecov':
  ensure   => ['0.18.5'],
  provider => 'gem',
}

package { 'simplecov-console':
  ensure   => ['0.4.2'],
  provider => 'gem',
}

package { 'simplecov-html':
  ensure   => ['0.12.3'],
  provider => 'gem',
}

package { 'singleton':
  ensure   => ['0.1.0'],
  provider => 'gem',
}

package { 'six':
  ensure   => '1.15.0',
  provider => 'pip',
}

package { 'spdx-licenses':
  ensure   => ['1.3.0'],
  provider => 'gem',
}

package { 'specinfra':
  ensure   => ['2.82.2'],
  provider => 'gem',
}

package { 'stomp':
  ensure   => ['1.4.4'],
  provider => 'gem',
}

package { 'stringio':
  ensure   => ['0.1.0'],
  provider => 'gem',
}

package { 'strings':
  ensure   => ['0.1.8'],
  provider => 'gem',
}

package { 'strings-ansi':
  ensure   => ['0.2.0'],
  provider => 'gem',
}

package { 'strscan':
  ensure   => ['1.0.3'],
  provider => 'gem',
}

package { 'table_print':
  ensure   => ['1.5.7'],
  provider => 'gem',
}

package { 'terminal-table':
  ensure   => ['3.0.2'],
  provider => 'gem',
}

package { 'test-unit':
  ensure   => ['3.3.4'],
  provider => 'gem',
}

package { 'text':
  ensure   => ['1.3.1'],
  provider => 'gem',
}

package { 'thor':
  ensure   => ['1.1.0', '1.0.1'],
  provider => 'gem',
}

package { 'timeout':
  ensure   => ['0.1.0'],
  provider => 'gem',
}

package { 'toml':
  ensure   => '0.10.2',
  provider => 'pip',
}

package { 'tomlrb':
  ensure   => ['2.0.1'],
  provider => 'gem',
}

package { 'tracer':
  ensure   => ['0.1.0'],
  provider => 'gem',
}

package { 'tty-cursor':
  ensure   => ['0.7.1'],
  provider => 'gem',
}

package { 'tty-pager':
  ensure   => ['0.13.0'],
  provider => 'gem',
}

package { 'tty-screen':
  ensure   => ['0.8.1'],
  provider => 'gem',
}

package { 'tty-spinner':
  ensure   => ['0.9.3'],
  provider => 'gem',
}

package { 'typing_extensions':
  ensure   => '4.0.1',
  provider => 'pip',
}

package { 'unf':
  ensure   => ['0.1.4'],
  provider => 'gem',
}

package { 'unf_ext':
  ensure   => ['0.0.8'],
  provider => 'gem',
}

package { 'unicode-display_width':
  ensure   => ['1.8.0'],
  provider => 'gem',
}

package { 'unicode_utils':
  ensure   => ['1.4.0'],
  provider => 'gem',
}

package { 'uri':
  ensure   => ['0.10.0'],
  provider => 'gem',
}

package { 'webrick':
  ensure   => ['1.6.1'],
  provider => 'gem',
}

package { 'wheel':
  ensure   => '0.33.1',
  provider => 'pip',
}

package { 'windows_error':
  ensure   => ['0.1.2'],
  provider => 'gem',
}

package { 'winrm':
  ensure   => ['2.3.6'],
  provider => 'gem',
}

package { 'winrm-fs':
  ensure   => ['1.3.5'],
  provider => 'gem',
}

package { 'with_env':
  ensure   => ['1.1.0'],
  provider => 'gem',
}

package { 'wrapt':
  ensure   => '1.13.3',
  provider => 'pip',
}

package { 'xml-simple':
  ensure   => ['1.1.9'],
  provider => 'gem',
}

package { 'xmlrpc':
  ensure   => ['0.3.0'],
  provider => 'gem',
}

package { 'yaml':
  ensure   => ['0.1.0'],
  provider => 'gem',
}

package { 'yard':
  ensure   => ['0.9.26'],
  provider => 'gem',
}

package { 'zlib':
  ensure   => ['1.1.0'],
  provider => 'gem',
}
